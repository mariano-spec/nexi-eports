[build]
  command = "npm run build"
  functions = "netlify/functions"
  publish = "dist"

[dev]
  command = "npm run dev"
  port = 3000

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
```

**MUY IMPORTANTE:**
- Sin espacios raros
- Sin caracteres especiales
- Exactamente como lo ves aquí

---

### Pas 3: Commit

1. En **"Commit message"** escribe:
```
   Fix netlify.toml syntax error
